port: 4222
http_port: 8222
# Keep local conformance fragments below fabric's 4 MiB pooled transport buffer.
max_payload: 4MB
jetstream {
  store_dir: "/data"
  max_memory_store: 64MB
  max_file_store: 256MB
}
